pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{js,ts,json}"
      run: |
        PATH="$HOME/.bun/bin:$PATH" bun run check {staged_files}
        git add -u
    git-add:
      glob: "*.{js,ts,json}"
      run: git add {staged_files}
      stage_fixed: true
